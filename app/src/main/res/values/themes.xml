<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TubeSave" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <item name="android:windowBackground">@color/background_color</item>
        <item name="android:windowDisablePreview">true</item>

        <item name="colorOnBackground">@color/background_color</item>
        <item name="colorPrimary">@color/text_color</item>
        <item name="colorOnSecondaryContainer">@color/text_color</item>
        <item name="colorOnPrimary">@color/brand_primary</item>
        <item name="colorOnTertiary">@color/text_grey</item>
        <item name="cardBackgroundColor">@color/card_background_color</item>
        <item name="colorOnSurface">@color/half_transparent_white</item>
        <item name="colorSurface">@color/half_transparent_black</item>
    </style>

    <style name="Theme.TubeSave" parent="Base.Theme.TubeSave" />
</resources>